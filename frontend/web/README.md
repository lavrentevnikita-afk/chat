# Frontend PWA (minimal)

This is a minimal Vite + React PWA skeleton. It connects to backend Socket.IO and REST endpoints.

Run:

```bash
cd frontend/web
npm install
npm run dev
```

For production build, run `npm run build` and serve `dist` with any static server or via Caddy.
